<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xuan.service.mapper.ArticleTagMapper">

    <!-- 批量插入文章标签关联 -->
    <insert id="batchInsertArticleTags"> INSERT INTO article_tag (article_id, tag_id, create_time)
        VALUES <foreach collection="list" item="item" separator=","> (#{item.articleId},
        #{item.tagId}, #{item.createTime}) </foreach>
    </insert>

    <!-- 根据文章ID删除标签关联 -->
    <delete id="deleteByArticleId"> DELETE FROM article_tag WHERE article_id = #{articleId} </delete>

    <!-- 根据文章ID查询标签ID列表 -->
    <select id="selectTagIdsByArticleId" resultType="java.lang.Long"> SELECT tag_id FROM article_tag
        WHERE article_id = #{articleId} </select>

</mapper>